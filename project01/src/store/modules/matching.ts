export interface MatchingState {
  tags: null;
}

const state: MatchingState = {
  tags: null,
};

const mutations = {};

const matching = {
  namespaced: true,
  state,
  mutations,
};

export default matching;
